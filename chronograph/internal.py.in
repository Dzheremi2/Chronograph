import os
from pathlib import Path

from dgutils.project_shared import AbstractSchema, ProjectShared
from gi.repository import GLib

_PREFIX: str = "@PREFIX@"

_prj = ProjectShared(
    _PREFIX + "/resources/internals.yaml",
    _PREFIX + "/resources/schema.yaml",
    Path(os.path.join(GLib.get_user_config_dir(), "chronograph", "schema")),
)
Constants = _prj.Constants
Schema: AbstractSchema = _prj.Schema
