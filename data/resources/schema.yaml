# yaml-language-server: $schema=../../docs/schemas/Schema-schema.json
root:
  settings:
    file-manipulation:
      enabled:
        type: bool
        default: true
      format:
        type: str
        enum: [.lrc, .txt]
        default: .lrc
      throttling:
        type: float
        min: 1.0
        max: 10.0
        default: 5.0
      elrc-prefix:
        type: str
        default: eLRC-
      lrc-along-elrc:
        type: bool
        default: true
      embed-lyrics:
        enabled:
          type: bool
          default: false
        default:
          type: str
          enum: [plain, lrc, elrc]
          default: lrc
        vorbis:
          type: bool
          default: true
    syncing:
      sync-type:
        type: str
        enum: [lrc, wbw]
        default: lrc
      precise:
        type: bool
        default: false
    general:
      reset-quick-editor:
        type: bool
        default: true
      save-session:
        type: bool
        default: false
      auto-list-view:
        type: bool
        default: false
      compressed-covers:
        enabled:
          type: bool
          default: false
        level:
          type: int
          min: 1
          max: 95
          default: 75
      recursive-parsing:
        enabled:
          type: bool
          default: false
        follow-symlinks:
          type: bool
          default: false
      debug-profile:
        type: bool
        default: false
  state:
    library:
      sorting:
        type: str
        enum: [a-z, z-a]
        default: a-z
      view:
        type: str
        enum: [g, l]
        default: g
      session:
        type: str
        default: None
    window:
      height:
        type: int
        default: 600
      width:
        type: int
        default: 700
      maximized:
        type: bool
        default: false